const removeChar = require("../src/removeFirstAndLastCharacterLiliia");
const expect = require("chai").expect;

describe("removeChar", () => {
    it("removeChar(contry) should return 'ontr'", () => {
      expect(monkeyCount(country)).to.be.equal('ontr');
    })

    it("removeChar(person) should return 'erso", () => {
        expect(removeChar(person)).to.be.equall('erso');
      })
})
